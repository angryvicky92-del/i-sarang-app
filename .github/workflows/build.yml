name: Build Android
on:
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
          cache-path: app/package-lock.json

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-cache: true
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        working-directory: app
        run: npm install

      - name: Build Android AAB (Free Local Build)
        working-directory: app
        run: |
          # Note: Using --local flag to bypass EAS cloud build limits
          # This uses the GitHub Runner's resources to build the app
          npx eas-cli build --platform android --profile production --local --non-interactive --output=./uljip_check_v2.aab

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-app-bundle
          path: app/uljip_check_v2.aab
